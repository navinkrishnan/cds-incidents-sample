{
  "name": "cds-incidents-sample",
  "version": "1.1.0",
  "description": "A CAP cds add plugin.",
  "repository": "https://github.com/navinkrishnan/cds-incidents-sample.git",
  "license": "Apache-2.0",
  "peerDependencies": {
    "@sap/cds-dk": ">=8"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "@sap/cds-dk": ">=8"
  },
  "scripts": {
    "test": "jest --silent test/*.test.js"
  }
}
